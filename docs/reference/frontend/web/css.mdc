---
description: 【参考/二开补充】原生 CSS 布局与变量；SCSS 项目请改用 scss.mdc
globs:
alwaysApply: false
---

# CSS（参考）

> **优先级**：本地样式约定 > 本文件。已用 SCSS 的项目优先只引用 `scss.mdc`。

## 原则

- 布局优先 Flex / Grid；少用随意绝对定位堆叠。  
- 用自定义属性（`--color-text` 等）集中主题色与间距。  
- 类名跟项目（BEM / 其他）保持一致，不混用多套命名法。  
- 响应式用清晰断点；避免大量 `!important`。

## 禁止

- 无必要的深层选择器（`div div div span`）。  
- 把关键布局尺寸写死成大量魔法像素且无变量。

## AI 行为

- 复用已有变量与工具类，不新造平行色板。  
- 不默认引入 Tailwind/UI 库，除非项目已有。
