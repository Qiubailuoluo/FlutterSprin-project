---
description: 【参考/二开补充】Vue 请求封装与错误处理；冲突时以本地 axios/fetch 封装为准
globs:
alwaysApply: false
---

# Vue 网络（参考）

> **优先级**：本地 axios/fetch 封装、拦截器 > 本文件。

## 约定

- 基址、超时、Token 头在单一 http 实例中配置。  
- 统一处理业务 `code` 与 HTTP 错误；页面只接「成功数据 / 抛错或 Result」。  
- API 按模块分文件，路径常量集中，避免组件内魔法字符串。

## 禁止

- 组件里每次 `fetch` 复制粘贴鉴权头。  
- 把服务端密钥写进前端环境并提交仓库（仅用 `VITE_` 等公开配置）。  

## AI 行为

- 新接口：先 `api/` 方法，再在页面/composable 调用。  
- loading / 错误提示复用项目现有 UI 库模式。
