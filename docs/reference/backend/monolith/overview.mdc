---
description: 【参考/二开补充】单体/模块化单体约定；微服务项目请忽略或删除本目录
globs:
alwaysApply: false
---

# 单体应用（参考）

> **优先级**：本地部署与模块结构 > 本文件。  
> **微服务项目**：删除或忽略整个 `backend/monolith/`，改用 `backend/microservices/`。

## 定位

- 默认一个可部署单元、一份主数据源（或项目已有的读写分离仍属单体数据面）。  
- 用包/模块划分业务（modular monolith），**不要**为了「看起来像微服务」而拆空服务。  
- 分层、异常、API、安全等仍用 `backend/common/`。

## 建议

- 模块边界清晰（按业务包），模块间依赖单向、避免循环。  
- 跨模块调用走 Service 接口，不直连他模块 Mapper（若项目已有约定则对齐）。  
- 将来若拆服务：先在单体中理清边界与数据所有权，再搬到 `microservices` 约定。

## AI 行为

- 用户声明单体时：用 `common` + 本文件；**不引用** `microservices/`。  
- 不主动引入注册中心、服务间 RPC、拆库，除非用户明确要求转型微服务。
