---
description: 【参考/二开补充】前端功能模块与多语言；触及即抽 key，不宣称全站完成
globs:
alwaysApply: false
---

# 前端模块与多语言（参考）

> **优先级**：本地已有 i18n 方案（gen-l10n / vue-i18n 等）> 本文件。  
> 目标：按**功能模块**组织文案与代码；切换语言时改资源与 locale，不改业务逻辑。

## 触及范围（避免验收幻觉）

- **触及即抽 key**：本轮新增或修改的用户可见字符串应走 i18n。  
- **未触及不强制**：不借机全项目翻译；acceptance 只勾「本轮触及处」。  
- 禁止在收尾写「项目已全面 i18n」，除非任务明确要求全量迁移且已完成。

## 功能模块

- UI 与文案按 **feature/模块** 边界组织（如 `auth`、`bill`、`settings`），与后端模块名尽量可对应。  
- 跨模块共用文案（确定、取消、加载失败）放 `common` / `core` 文案区。  
- 新功能：先定模块，再增加文案 key，禁止在无关模块塞字符串。

## 多语言原则（所有栈）

1. **本轮触及的**用户可见文案禁止硬编码在 Widget/模板里（合并前抽 key）。  
2. 使用 **稳定 key**（如 `bill.list.title`），不要用中文当 key。  
3. 每种语言一份资源，结构一致，缺译走 **fallback**。  
4. **切换语言**：一处设置 locale，全局生效；持久化用户选择。  
5. 日期、数字、货币用 locale 感知格式化。  
6. 后端 `message`：后端多语言 **或** 前端按错误码映射（二选一，跟项目）。

## 推荐资源布局

```text
l10n/ 或 i18n/
  zh/ common.json  bill.json
  en/ …
  ja/ …
```

或小项目单文件多 locale（如 Flutter ARB / 统一 `AppStrings` 表）。

## 各栈要点（勿混用两套）

| 栈 | 常见方案 |
|----|----------|
| Flutter | `gen-l10n` 或统一 `AppStrings` + locale 控制器 |
| Vue 3 | `vue-i18n`，按模块拆 messages |
| 原生 Web | 消息表 + 小型 i18n 模块 |

## 切换语言 Checklist（仅当本轮做了切换能力或新语言时）

- [ ] 新增语言 = 复制资源并翻译，本轮 key 齐全  
- [ ] Fallback 已配置  
- [ ] 可切换并刷新界面  
- [ ] **本轮触及 UI** 无残留硬编码  

## AI 行为

- 加/改页面文案时走 key；至少默认语言，预留 en（或项目指定）。  
- 二开硬编码：只抽触及字符串。  
- 不引入与项目冲突的第二套 i18n 框架。
