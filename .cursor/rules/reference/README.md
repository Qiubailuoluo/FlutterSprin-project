# reference — 运行副本

供 Cursor `@` 引用。**改规则请改全集源** `docs/reference/`，再同步到本目录。

## 开场（必做）

```text
@.cursor/rules/reference/project-bootstrap.mdc
```

入口文件名约定为 `project-bootstrap.mdc`；其他项目路径/产品名可不同，但开场仍应 `@` **入口协议**。

- 后端当前 = Java/Spring（见 `backend/README.md`）
- 演练流水：`docs/reference/DRILL.md`（可选、可清空）
- 文档分层：项目根 `docs/README.md` + `workflow/docs-sync.mdc`
