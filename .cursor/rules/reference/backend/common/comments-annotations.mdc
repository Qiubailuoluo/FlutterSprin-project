---
description: 【参考/二开补充】JavaDoc/文档注释与框架注解；利于 IDE、工具与 AI 检索
globs:
alwaysApply: false
---

# 注释与注解（参考）

> **优先级**：本地注释习惯与项目正式 rules > 本文件。  
> 文档注释（`/** … */`）可被 **Javadoc / IDE / 部分静态分析** 识别；合适的**注解**把契约交给框架与开发工具。二者都能让人与 AI **更快定位意图**，减少瞎猜。

## 为何有助于检索与开发

| 手段 | 作用 |
|------|------|
| JavaDoc（类/方法） | 说明职责、参数、异常、`@see` 文档路径；IDE 悬停、生成文档、搜索符号时可见 |
| 行内 `//` | 只解释「为什么」或非直观规则（Redis key、权限边界） |
| 框架/校验注解 | 声明路由、校验、事务、映射；编译期/运行期由框架执行，IDE 可提示 |

AI 改代码前应先读相关类的 JavaDoc 与注解，再动手（配合 `code-change`）。

## JavaDoc（多行文档注释）

- 使用 `/** … */`（不是 `/* … */` 普通块注释）写在**类、接口、public/protected 方法**上。  
- **类**：一句话职责 + 必要时 `@see docs/...` 或关联模块。  
- **方法**：用途、关键参数含义、可能的业务异常/空返回约定。  
- Controller 接口方法建议 `@see docs/api/xxx.md`。  
- 写「为什么 / 边界」，少写复述代码的「做了什么」。  
- **禁止**：无意义注释（如紧跟 `getUser()` 写 `// 获取用户`）；大段粘贴过期需求。

私有方法、一目了然的代码可不写 JavaDoc；复杂分支用短行内注释即可。

## 注解（开发环境与契约）

按项目已有栈选用，**不引入第二套同类注解**：

| 类别 | 示例（跟项目实际） | 作用 |
|------|-------------------|------|
| Web | `@RestController`、`@GetMapping`、`@RequestBody` | 路由与绑定 |
| 校验 | `@Valid`、`@NotNull`、`@Size` 等 | 入参契约，失败走统一异常 |
| 事务 | `@Transactional`（一般在 Service） | 事务边界 |
| 注入 | 构造器注入 / `@RequiredArgsConstructor` | 依赖关系清晰 |
| 持久化 | `@TableName`、`@TableId` 等 | 表映射 |

原则：

- 能表达的契约优先用注解 + JavaDoc，避免只靠口头约定。  
- 注解参数（路径、消息、size）与 `docs/api` 保持一致。  
- 不要堆砌无运行效果的「装饰性」自定义注解。

## 与 docs 的关系

- JavaDoc 里的 `@see` 指向仓库文档；**详细契约仍以 `docs/` 为准**并按 `docs-sync` 更新。  
- 注释不能替代 API 文档；两者互补：代码侧便于跳转检索，docs 侧便于前后端对齐。

## 其他语言（同库多栈时）

- **Dart/Flutter**：公开 API 用 `///` Dartdoc；关键 Widget/方法写清职责。  
- 细节见对应 `frontend/<栈>/`；本文件以 Java/Spring 为主。

## AI 行为

- 新增/修改 public API 时同步补全或更新 JavaDoc 与必要注解。  
- 二开时优先阅读并保留仍准确的注释；过时注释要改对，不要留错误指引。  
- 不为「看起来规范」给每一行加注释。
