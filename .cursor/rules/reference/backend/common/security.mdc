---
description: 【参考/二开补充】后端安全：密码、鉴权、授权、密钥；冲突时以本地 Security 为准
globs:
alwaysApply: false
---

# 后端安全（参考）

> **优先级**：本地 `Security`/`Filter`、JWT 封装、配置项 > 本文件。  
> 不写死 Token 过期秒数、Redis key 名、白名单路径——跟项目现有实现。

## 密码与凭证

- 密码使用强哈希（如 BCrypt）；禁止明文存储、禁止打进日志。  
- 响应与 VO 不得返回密码哈希或密钥字段。  
- 配置中的密钥、数据源密码走环境变量/配置中心，不提交真实值。

## 鉴权（Authentication）

- 登录态校验放在统一入口（Filter / Interceptor / Security），业务接口默认需认证。  
- 匿名路径（登录、注册、健康检查等）集中配置，新增公开接口必须同步白名单。  
- Controller 取「当前用户」用项目上下文工具；不在各接口重复解析 Token。

## 授权（Authorization）— 管理员 / 角色

- **角色与权限在服务端强制校验**（通常在 Service 查库或可信上下文）；禁止只靠前端隐藏菜单。  
- JWT claim 中的 role 可用于展示与快速判断；**敏感管理操作建议再读库确认**（防角色变更后旧 Token 越权）。  
- 资源归属（是否本人数据）与角色门闩分开：普通 CRUD 校归属；管理接口校角色。  
- 封禁/禁用用户：更新状态后**立即失效会话**（如删 Redis Token）；禁用账号不可登录。  
- 禁止管理员「封自己」等明显危险自操作（业务上拦截）。  
- 前端管理入口仅作 UX；接口仍须返回 403 给无权限调用方。

## Token / 会话

- 前端只持客户端可用的 Token；**禁止**把服务端 AccessKey/私钥下发浏览器。  
- 登出或失效策略跟项目（如 Redis 剔 Token）；写相关代码前先读现有实现。  
- 过期时间、刷新机制以项目配置为准，本参考不规定具体数值。

## 接口与数据

- 入参校验（`@Valid` 等）；敏感操作考虑权限与幂等。  
- 错误消息不泄露堆栈、SQL、内部路径（见 `exception`）。  
- 上传/下载若走对象存储，遵循 `middleware/minio`（私有桶、预签名、路径隔离）。

## AI 行为

- 改安全相关代码前先读现有 Security 配置与过滤器。  
- 新增需登录接口时检查是否误入公开名单。  
- 新增管理端能力时：同时做 Service 授权 + 文档说明角色要求 + 前端按角色显隐（三者缺一不可）。  
- 不编造与仓库不一致的第二套鉴权方案。
