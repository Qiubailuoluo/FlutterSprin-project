---
description: 【参考/二开补充】改动收尾验收；最低验证门槛与基础设施测试约定
globs:
alwaysApply: false
---

# 验收 / 收尾清单（参考）

> **优先级**：项目 CI、PR 模板、正式 DoD > 本文件。  
> 用于对话收尾自检；不是完整测试规范或项目管理流程。

## 最低验证（有则必做，避免空勾）

在适用时至少完成对应一项，并在收尾说明结果：

| 改动类型 | 最低验证 |
|----------|----------|
| 新增/修改 HTTP 接口 | 至少 **1 次成功** + **1 次预期失败**（如 400/401/403），或运行已有覆盖该接口的测试 |
| 新增/修改前端页面或关键交互 | `analyze`（或项目等价检查）无新增问题 + 主路径手测一句 |
| 表结构 / 迁移 | 迁移已执行或说明如何执行；与 `schema`/文档一致 |
| 文案 / i18n | 仅核对本轮**触及**的 key/界面（见 `frontend/i18n`） |

未做项必须如实写明，禁止写「已验收」却无任何验证痕迹。

## 基础设施改动 → 必须带自动化测试

以下属于**公共基础设施**（改动影响面大），收尾前须有自动化测试（单元/切片即可，不必上全链路 E2E）：

- 统一响应 `Result` / 错误码序列化  
- 全局异常 Advice、`errorId` / requestId 过滤器  
- 鉴权 Filter、Token 校验核心工具  
- 其他被多模块依赖的 common 工具（项目认定）

普通业务 CRUD：文档 + 最低验证（实调）即可，**不**强制全站单测（除非项目 CI 要求）。

## 通用（每次有意义的改动）

- [ ] 行为与需求一致；已做上方适用的最低验证。  
- [ ] 未引入密钥、明文密码、生产地址进仓库。  
- [ ] 错误路径有合理提示（不吞异常、不堆栈回传客户端；500 见 `exception` + `errorId`）。  
- [ ] 命名与分层对齐本地现有风格。  
- [ ] 若项目有 `docs/`（或本次应创建）：相关说明已更新（见 `docs-sync`）。

## 后端接口

- [ ] 入参校验、统一响应/错误码与项目一致。  
- [ ] 需登录接口未误配为匿名；**归属与角色**校验在 Service（见 `security`）。  
- [ ] 新增/改动的 public API 有恰当 JavaDoc 与必要注解（见 `comments-annotations`）。  
- [ ] 接口 / 表结构变更已同步 `docs/api`、`docs/database`（或等价路径）。

## 前端

- [ ] 请求走统一封装；未在 UI 硬编码生产基址或密钥。  
- [ ] 加载 / 错误态与现有页面模式一致；500 用安全文案 + `errorId`（见网络规则）。  
- [ ] 只引用了当前栈的规则，未混用其他框架约定。  
- [ ] **本轮触及的**用户可见文案已走 i18n key；未触及处不强制、不宣称「项目已全面 i18n」。

## 缓存 / 存储（若本次涉及）

- [ ] Key/路径含正确隔离（用户/租户）；有 TTL 或生命周期说明。  
- [ ] 写库后缓存失效策略与项目一致。

## 分阶段（与 task-breakdown 配套）

- **每个 P 结束**：本 P 相关轻量自检 + 检查点；一口气模式也要勾落盘。  
- **整段需求收尾**：过适用勾选项 + 最低验证表。  
- 分步暂停时：注明未做整段 acceptance。

## AI 行为

- 按 `reference` 开发的任务：整段收尾必须过适用项；未做项如实说明。  
- 不把本清单当成「业务功能必须写满自动化测试」的强制令；**基础设施例外**（见上节）。
