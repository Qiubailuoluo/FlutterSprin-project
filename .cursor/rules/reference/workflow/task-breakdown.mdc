---
description: 【参考/二开补充】任务拆解：P1…Pn、混合执行、清单必出与进度落盘
globs:
alwaysApply: false
---

# 任务拆解（参考）

> **优先级**：用户给出的需求边界与项目里程碑 > 本文件。  
> 与 `project-bootstrap` 配套：**先拆任务 → 每 P 选主规则 → 实现 → 收尾**。

## 为何拆解

- 一次塞进过多目标 → 规则选不准、上下文乱、漏 docs/验收。  
- 拆开后每个子任务职责清晰，才能保持检索高效与实现约束一致。  
- **断线可恢复**：有编号阶段与落盘进度时，新对话可从「下一 P」续作，不依赖完整聊天记忆。

## 拆解原则

1. **一个子任务一个可验收结果**（如「登录 API 可调通」，不是「把整个用户中心做完」）。  
2. **按依赖排序**：库表/契约 → 后端 → 前端 → 联调 → 文档与收尾。  
3. **每个 P 单独选主规则**（见下方「每 P 主规则」），不要整包 reference 一次挂满。  
4. **二开**：先标「改哪些现有模块」，子任务对齐现有边界，不顺手重构无关模块。  
5. 子任务仍太大就再拆；编号建议 **P1…Pn（约 2～5）**；同一需求内统一用 P 前缀（勿与 T 混用）。

## 需求不清时（先澄清，再拆 P / 再改代码）

用户表述不够清晰，或检查代码后发现风险时：

1. **先多轮对话确认**，不要假设需求后直接改。  
2. 说明：问题/风险、可选方案、预期结果、待确认点（细则见 `workflow/code-change`）。  
3. 用户确认（或明确授权「按推荐方案做」）后，再输出 P 清单并进入实现。  

边界已清楚的小改动、或用户已给足约束且说「直接做」：可跳过长澄清，但仍建议一句话复述预期结果。

## 开场必做（弱确认 + 落盘）

动手写业务代码前，**必须**先输出短清单（可与用户首条合并在同一回复，但须可见）：

```text
模式：分步 | 一口气（依据：用户原话 / 默认）
[ ] P1 …（本 P 主规则：a · b）
[ ] P2 …
进度落盘：docs/…/DRILL.md 或项目阶段笔记（无则本对话检查点亦可，推荐落盘）
```

- **清单与模式必出**；是否中途暂停见「执行模式」。  
- 大需求且用户**未**表达一口气/自测后验等语义时：默认 **分步**；完成 P1 检查点后暂停，等「继续」。  
- 用户已说「一口气 / 中途不要停 / 自己测完我再验」等：按 **一口气**，仍须列 P 并在过程中勾选落盘。  
- 小需求：可不拆 P，或只列 1 项；不必为走流程硬拆。

## 执行模式（混合）

| 模式 | 何时用 | AI 行为 |
|------|--------|---------|
| **分步** | 大需求默认；或用户明确「按 P 分步」 | 只做当前 P → 检查点 → **暂停**等「继续」 |
| **一口气** | 小需求默认；或用户明确一口气/自测后验 | 按 P 顺序做完；**不强制**每 P 停；仍要检查点与落盘勾选 |
| **用户覆盖** | 任意时刻 | 「分步」↔「一口气」立即切换 |

说明：假「默认分步」却一口气做完且无清单/落盘 = **违规**。一口气允许不停，不允许不拆、不记进度。

## 每 P 主规则（替代「整需求只挂 1～3」的 KPI）

- **每个 P** 显式声明 **1～3 个**本 P 主专题 `.mdc`（可 `@`）。  
- `project-bootstrap` / `task-breakdown` / `code-change` 为流程件，可不计入这 1～3。  
- 其余能力靠**已裁剪目录**与本地代码惯例，禁止整包粘贴 reference。  
- 跨 P 可换主规则（如 P1=mysql+docs，P2=layering+api）。

## 建议拆分模板

```text
模式：分步 | 一口气
[ ] P1 契约/数据：…（主规则：docs-sync · mysql）
[ ] P2 后端：…（主规则：code-change · layering）
[ ] P3 前端：…（主规则：frontend/<栈> · i18n?）
[ ] P4 联调：…（主规则：security · acceptance）
[ ] P5 文档与收尾：…（主规则：docs-sync · acceptance）
```

按实际裁剪行数。

## 检查点（每个 P 结束时）

1. 本 P 可验收结果  
2. 关键路径  
3. 如何验证（命令或手测；见 `acceptance` 最低验证）  
4. 下一 P；分步则写明「回复继续以开始 Px」  
5. **进度落盘**：更新练习/阶段笔记勾选（推荐）；无笔记则对话内保留完整剩余清单  

Git 提交仅当用户要求；检查点 ≠ 自动提交。

## AI 行为

- 跨多层/多模块：**先清单 + 模式**，再编码。  
- 断线续作：读落盘进度与 git 未提交改动，从下一未完成 P 继续并复述剩余清单。  
- 不写冗长项目管理文案；清单短、可执行即可。
