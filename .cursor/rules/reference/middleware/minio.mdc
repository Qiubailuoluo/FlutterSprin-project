---
description: 【参考/二开补充】MinIO/S3 兼容对象存储约定；冲突时以本地配置与 SDK 为准
globs:
alwaysApply: false
---

# MinIO（S3 兼容）参考规范（泛类）

> **参考优先级**：项目已有 bucket、路径、客户端封装、配置项 > 本文件。  
> 无对象存储需求的项目可忽略；有则先读配置与现有上传下载实现。

## 适用场景

用户上传（头像/附件）、导出文件、需跨服务共享的二进制内容等。  
元数据与业务关联放数据库；对象存储只存文件本体。

## 1. 适合 / 不适合

**适合**：静态与半静态文件、导出包、归档类对象（配合生命周期）。  

**不适合**：当数据库用；强一致频繁改写的临时碎片文件；把 AccessKey 下发到浏览器。

## 2. Bucket 与权限

- 命名建议：小写 + 中划线，带环境或业务，如 `dev-user-avatars`。  
- 默认**私有**；公开读需求优先预签名 URL 或 CDN 回源，源桶仍私有。  
- 服务端用配置/环境变量持有密钥；**禁止**把 AccessKey/SecretKey 写进前端或仓库明文。

## 3. 路径与隔离

推荐体现业务与归属，例如：

```text
users/{userId}/{biz}/{uniqueName}
tenants/{tenantId}/{biz}/{uniqueName}
```

- 对象名用 UUID/雪花等唯一名 + 扩展名；原始文件名只进 DB，不直接当对象名。  
- 路径勿含手机号、身份证等敏感明文。  
- 读写下发凭证前校验当前用户/租户是否有权访问该路径。

## 4. 上传下载安全

- 服务端校验大小上限与类型（MIME + 魔数/白名单，不只看扩展名）。  
- 预签名：路径由服务端指定；下载/上传分别设合理短有效期。  
- 业务记录建议落库：bucket、object key、原名、大小、MIME、校验和、上传者、关联业务 ID。

## 5. 生命周期

- 临时导出等设生命周期或定时清理。  
- 用户文件删除：业务侧先软删/不可访问，再异步删对象；对齐项目现有策略。

## 6. AI 行为（参考）

涉及对象存储时说明：bucket、路径规则、预签名时长、大小/类型校验是否齐全。  
缺配置或路径约定时先问，不编造生产 bucket 名；密钥只走配置读取。
