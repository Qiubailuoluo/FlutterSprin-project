---
description: 【参考/二开补充】Redis 缓存与 Key 约定；冲突时以本地现有 key 与封装为准
globs:
alwaysApply: false
---

# Redis 参考规范（泛类）

> **参考优先级**：项目已有 Key 格式、封装类、TTL 常量 > 本文件。  
> 二开时先全局搜索现有 `Redis`/`StringRedisTemplate` 用法再落笔。

## 适用场景

热点缓存、会话/令牌附属状态、限流计数、分布式锁、短期幂等键等。  
不是「凡读写都进 Redis」；不确定是否该缓存时先问或先查现有实现。

## 1. 适合 / 不适合

**适合**：高频读低频写、可接受短暂不一致的数据；验证码/限流；短时锁；排行榜/计数器。  

**不适合**：大文件/大文本本体（存指针）；强事务多表关系；不能丢的核心业务事实（除非项目已明确用 Redis 持久化并接受风险）；极冷数据。

## 2. Key 设计

### 2.1 推荐格式

```text
{业务模块}:{实体}:{业务标识}[:属性]
```

全小写、冒号分段；示例：`user:info:123`、`lock:pay:order:456`。

### 2.2 隔离前缀（按业务需要）

| 范围 | 示意 | 说明 |
|------|------|------|
| 全局/系统 | `sys:` / `global:` | 全站配置等 |
| 用户 | `user:{userId}:` / `uid:{userId}:` | 禁止跨用户读写 |
| 租户 | `tenant:{tenantId}:` | 多租户时必须带租户 |

生成代码时 Key 必须能看出权限边界；用户输入不可直接拼进 Key，先校验或映射。

> 示例（某项目曾用）：`auth:token:{userId}`、`stats:user:{userId}:month:{yyyy-MM}` — **二开时以当前项目正式规则与代码为准**，勿照搬 key 名。

## 3. TTL 与更新

- 业务 Key **默认都要过期**；真正需长期保留的也要有刷新策略。  
- TTL 用命名常量，避免魔法秒数散落。  
- 写库后缓存：常见「先写库再删缓存」或项目已采用的双删/更新策略，**对齐现有代码**。  
- 穿透：短 TTL 缓存空值；击穿：互斥加载或已有锁封装。

## 4. 使用边界

- 连接走连接池/项目封装；生产禁用 `KEYS *`、`FLUSHALL`、`FLUSHDB`。  
- 批量优先 `MGET`/`pipeline`；复杂原子操作用 Lua 时保持脚本短小。  
- 分布式锁：必须设过期；释放要安全（谁加锁谁释放 / 项目锁工具）。

## 5. AI 行为（参考）

生成 Redis 相关代码时一并说明：Key 形态与隔离级别、TTL、失效/更新策略。  
写锁必须含超时与释放路径。有现成工具类则复用，不新造一套 API。
