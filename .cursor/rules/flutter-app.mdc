---
description: Flutter 前端分层与编码规范（Windows/Web）
globs: flutter_app/**
alwaysApply: false
---

# Flutter 规范（Windows + Web）

## 目录结构

```
lib/
  app/           # 入口、路由、主题
  core/          # 网络、存储、ApiResult、常量
  features/      # 按业务划分
    {feature}/
      data/        # API 客户端、Model
      presentation/ # 页面、Widget
```

模块：`auth`、`bill`、`category`、`stats`。

## 分层职责

| 层 | 职责 |
|----|------|
| presentation | UI、状态展示、用户交互，不拼 URL |
| data | HTTP 请求、JSON 解析、Model 定义 |
| core | Dio 拦截器（注入 Token）、统一 `ApiResult` 解析 |

## 网络

- 基址：`http://localhost:8080`（开发环境）
- 解析后端 `{ code, message, data }`；`code != 200` 走统一错误提示
- Token 存 `shared_preferences`，请求头 `Authorization: Bearer {token}`

## 平台

- 当前目标：**Web（Edge）**；Windows 桌面暂缓（需 Visual Studio C++）
- 布局适配桌面宽度（NavigationRail + 宽内容区）

## 文档同步（改代码必做）

与 backend 相同原则，**代码与 docs 同一次提交**：

| 变更 | 更新 |
|------|------|
| 对接接口、字段 | `docs/api/` |
| 页面流程、路由 | `docs/learn/` 或 `docs/conventions.md` |
| 依赖、运行命令 | `docs/dev-setup.md` |

## 注释

- **类/文件顶部**：说明模块用途
- **public 方法/Widget build**：`///` 文档注释或简短说明
- **复杂逻辑行**：行内 `//`（状态流转、边界条件）
- Model 字段：`///` 注释含义，与 `docs/api` 一致
