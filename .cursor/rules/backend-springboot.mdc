---
description: Spring Boot 后端分层与编码规范
globs: backend/**
alwaysApply: false
---

# 后端规范（Spring Boot + MyBatis-Plus）

## 包结构

根包：`com.example.ledger`，**先按技术分层，再按业务模块**：

```
controller/   → login, user, bill, category, stats ...
service/      → login, user, bill ...
service/impl/ → login, user, bill ...
mapper/       → user, bill ...
entity/       → user, bill ...
dto/          → login, bill ...
vo/           → login, user, bill ...
```

业务模块示例：`login`、`user`、`bill`、`category`、`stats`。

公共代码放 `common/`（Result、异常、常量、工具）和 `config/`。

## 分层职责

| 层 | 允许 | 禁止 |
|----|------|------|
| Controller | 参数校验、调用 Service、返回 `Result<T>` | 业务逻辑、SQL、直接操作 Redis/Mapper |
| Service | 业务逻辑、事务、调用 Mapper/Redis | 处理 HTTP、返回原始 Entity 给前端 |
| Mapper | 数据访问（MyBatis-Plus 或 XML） | 业务判断 |

## 统一响应与异常

- 所有接口返回 `Result<T>`，成功 `code=200`
- 业务异常抛自定义异常，由 `@RestControllerAdvice` 统一处理
- 禁止在 Controller 内 try-catch 吞掉异常

## 安全

- 密码使用 BCrypt，禁止明文存储或日志输出
- JWT 有效期 1 天；Token 存 Redis key：`auth:token:{userId}`
- 鉴权：校验 JWT 且 Redis 中存在对应 Token

## Redis

- 统计缓存 key：`stats:user:{userId}:month:{yyyy-MM}`，TTL 5 分钟
- 账单增删改后删除对应用户当月统计 key

## SQL

- 建表脚本维护在 `docs/database/schema.sql`
- 优先 MyBatis-Plus 内置方法；复杂查询才写 XML

## 注释与 API 文档

### 必须写注释的位置

- **类**：JavaDoc，说明职责与关联文档 `@see docs/...`
- **public 方法**：JavaDoc（用途、参数含义、可能抛出的 BusinessException）
- **非直观代码行**：行内 `//` 注释（业务规则、Redis key、为何这样写）

### 注释原则

- 写「为什么」，少写「做了什么」（代码本身应可读）
- Controller 方法注明 `@see docs/api/xxx.md`
- 禁止无意义注释（如 `// 获取用户` 紧跟 `getUser()`）

### 文档同步（与代码同次提交）

| 变更 | 更新 |
|------|------|
| 接口 | `docs/api/` |
| 表结构 | `docs/database/schema.sql` |
| 机制/流程 | `docs/learn/` |
| 环境/命令 | `docs/dev-setup.md` |
